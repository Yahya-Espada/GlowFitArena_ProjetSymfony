{% extends '/admin2/base.html.twig' %}


{% block body %}
	<div class="content-body">

	<h1>Seance</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ seance.id }}</td>
			</tr>
			<tr>
				<th>Heuredebut</th>
				<td>{{ seance.heuredebut }}</td>
			</tr>
			<tr>
				<th>Heurefin</th>
				<td>{{ seance.heurefin }}</td>
			</tr>
			<tr>
				<th>Date</th>
				<td>{{ seance.date }}</td>
			</tr>
			<tr>
				<th>Prix</th>
				<td>{{ seance.prix }}</td>
			</tr>
			<tr>
				<th>Typeseance</th>
				<td>{{ seance.typeseance }}</td>
			</tr>
		</tbody>
	</table>
	
    <a href="{{ path('app_seance_index') }}">Retour à la liste</a>

    <a href="{{ path('app_seance_edit', {'id': seance.id}) }}">Modifier</a>

    {{ include('seance/_delete_form.html.twig') }}


	<a href="{{ path('app_seance_edit', {'id': seance.id}) }}">edit</a>

	{{ include('seance/_delete_form.html.twig') }}
{% endblock %}

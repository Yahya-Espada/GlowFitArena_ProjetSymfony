<form method="post" action="{{ path('app_subscriber_info_delete', {'id': subscriber_info.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subscriber_info.id) }}">
    <button class="btn btn-danger"></button>
</form>

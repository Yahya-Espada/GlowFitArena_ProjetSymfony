

<form method="post" action="{{ path('app_dietary_programs_copy_delete', {'id': dietary_programs_copy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dietary_programs_copy.id) }}">
    <button class="btn btn-danger" style="display: inline;" >Delete</button>
</form>

